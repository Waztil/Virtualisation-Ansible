- name: Configurer les VLANs sur VyOS
  vyos.vyos.vyos_interface:
    name: "{{ item.interface }}"
    address: "{{ item.address }}"
    state: present
  loop: "{{ vlans }}"

- name: Sauvegarder la configuration
  vyos.vyos.vyos_system:
    state: saved